services:
  frontend:
    build: .
    image: netvis-frontend:latest
    container_name: netvis-frontend
    ports:
      - "21080:80"
    restart: always
    environment:
      - VITE_API_BASE_URL=/api
      - VITE_USE_MOCK=true
